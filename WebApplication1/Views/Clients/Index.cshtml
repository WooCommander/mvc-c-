@model IEnumerable<ConsoleAppStart.model.Client>
@using WebApplication1.Models
@{
    ViewBag.Title = "Index";
}

<h2>Наши клиенты</h2>

<p>
    @Html.ActionLink("Добавить нового клиента", "Create")
</p>
<table class="table">
    <tr>
        <th>@Html.ActionLink("ФИО ↑", "Index", new { sortOrder = SortState.NameDesc })
        @Html.ActionLink("↓", "Index", new { sortOrder = SortState.NameAsc })
            @*<a asp-action="Index" asp-route-sortOrder="@ViewBag.NameSort">
                ФИО</a>*@
</th>
        <th>
            <a href="/Clients?sortOrder=@SortState.CompanyDesc"> Наименование компании ↑</a><a href="/Clients?sortOrder=@SortState.CompanyAsc"> ↓</a>


        <th>
        <th>
            @Html.ActionLink("Город ↑", "Index", new { sortOrder = SortState.CityAsc })
        @Html.ActionLink(" ↓", "Index", new { sortOrder = SortState.CityDesc })
    </th>
        
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FIO)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NameCompany)
        </td>
       
        
        <td>
            @Html.DisplayFor(modelItem => item.City)
        </td>
        
        <td>
            @Html.ActionLink("Изменить", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Подробнее", "Details", new { id=item.Id }) |
            @Html.ActionLink("Удалить", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
