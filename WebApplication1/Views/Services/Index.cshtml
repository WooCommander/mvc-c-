@model IEnumerable<ConsoleAppStart.model.Services>

@{
    ViewBag.Title = "Index";
}


<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Добавить новую", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
               Наименование услуги
            </th>
            <th>
               Цена
            </th>
            @*<th>
                Код заказа
            </th>*@
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NameSercvice)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PrceService)
            </td>
            @*<td>
                @Html.DisplayFor(modelItem => item.ServiceCode)
            </td>*@
            <td>
                @Html.ActionLink("Изменить", "Edit", new { id=item.Id }) |
                @Html.ActionLink("Подробнее", "Details", new { id=item.Id }) |
                @Html.ActionLink("Удалить", "Delete", new { id=item.Id })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
